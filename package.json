{
  "name": "polymer-router-demos",
  "description": "Demo routing with Polymer",
  "version": "0.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/erikringsmuth/polymer-router-demos"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.7.2"
  },
  "engines": {
    "node": "0.10.x"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "prep-static": "rm -f ./.gitignore && bower install",
    "deploy": "gh-pages-deploy"
  },
  "devDependencies": {
    "gh-pages-deploy": "^0.3.0"
  },
  "gh-pages-deploy": {
    "prep": [
      "prep-static"
    ]
  }
}
